"""
dictionary review

dictionaries compared / contrasted w/ other types:

* mutable (like lists, but unlike strings and tuples)
* unordered (order is not guaranteed, unlike all sequence types... lists, strings, tuples)
* key is used to access values
    * key / value pairs
    * (name / value pairs)
* syntax:
    * {}'s
    * {} # empty dictionary
    * {key1: value1, key2: value2} colon joins together key and value pairs
    * each key/val pair is separated by comma
* values can be ANY type... and the keys??? they have to immutable (strings, tuples, ints, floats_
""" 
